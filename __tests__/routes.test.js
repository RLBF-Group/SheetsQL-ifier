// import routers/endpoints from files throughout the project to test them here
// also need supertest
const supertest = require('supertest');
const server = require('../server/server.js');

describe("server on startup", () => {
  
})